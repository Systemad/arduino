#ifndef COMMON_H_
#define COMMON_H_

/*
 * Global variables 
 */
 
extern int redValue; // = 254;
extern int greenValue; // = 1;
extern int blueValue; // = 255;

extern int rDirection; // = -1;
extern int gDirection; // = 1;
extern int bDirection; // = -1;

//extern int fadeAmount; // = 1;
//extern int brightness; // = 0;

extern int keyOneCounter; // = 0;
extern int keyTwoCounter; // = 0;

extern unsigned long fadeMillis;

#endif
